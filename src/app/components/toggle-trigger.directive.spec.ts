import { ToggleTriggerDirective } from './toggle-trigger.directive';

describe('ToggleTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
